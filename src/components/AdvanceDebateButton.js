import React from 'react';

const AdvanceDebateButton = ({ onClick }) => {
    return (
        <button className="advance-debate-button" onClick={onClick}>
            Advance Debate
        </button>
    );
};

export default AdvanceDebateButton;
